[package]
name = "driver-engine"
version = "0.1.0"
edition = "2024"

[dependencies]
async-tpc-executor = { path = "../async-tpc-executor" }
driver-db = { path = "../driver-db" }
driver-util = { path = "../driver-util" }
tracing = "0.1"

[dev-dependencies]
futures-lite = "2.6"
serde = "1"
